import Sidebar from '../../components/Sidebar/Sidebar';

const Chat = () => {
    return (
        <div className="chatContainer">
            <Sidebar />
            <div>Messagerie</div>
        </div>
    );
};

export default Chat;
